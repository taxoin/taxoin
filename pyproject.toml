[project]
name = "gitchain"
version = "0.1.0"
description = "Blockchain on git — Bitcoin + Ethereum concepts with async transactions"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
